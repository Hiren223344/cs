# v3.3.4 — Anthropic error mapping

- Map upstream rate limits in non-streaming `/v1/messages` responses to HTTP
  429 instead of an opaque 500.
- Map upstream empty responses and protocol hint errors to HTTP 502, matching
  the OpenAI endpoint behavior.
- Add regression tests and include them in CI.
